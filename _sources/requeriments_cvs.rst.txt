Control de versions
===================

Una altra aplicació que farem servir és el control de versions ``git``. És
tracta d'una eina que et permetrà conservar canvis que vagis realitzant en els
teus programes. Pots comprovar si ja la tens instal·lada:

.. code-block:: console

    $ git --version

En cas que et respongui amb un missatge d'error de l'estil ``-bash: git:
command not found``, pots instal·lar ``git`` amb:

.. code-block:: console

    $ sudo apt install git
